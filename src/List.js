import React, { Component } from 'react'

const { fetch } = window

class List extends Component {
  render () {
    return (
      <div>
        Hello
      </div>
    )
  }

  componentDidMount () {
  }
}

export default List
